# setup.py
from setuptools import setup, find_packages

setup(
    name="mia_libreria",
    version="0.1",
    description="Una libreria di esempio per Python",
    author="Il Tuo Nome",
    author_email="tua_email@example.com",
    packages=find_packages(),
    install_requires=[],  # Aggiungi le dipendenze qui
)
